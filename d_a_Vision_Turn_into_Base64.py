import base64
import requests

# OpenAI API Key
api_key = "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"

# Function to encode the image
def encode_image(image_path):
  with open(image_path, "rb") as image_file:
    return base64.b64encode(image_file.read()).decode('utf-8')

# Path to your image
image_path = "pages/picture/TestVision1.jpg"

# Getting the base64 string
base64_image = encode_image(image_path)

headers = {
  "Content-Type": "application/json",
  "Authorization": f"Bearer {api_key}"
}

payload = {
  "model": "gpt-4-vision-preview",
  "messages": [
    {
      "role": "user",
      "content": [
        {
          "type": "text",
          "text": "Please help us interpret the meaning of the picture from a professional point of view, which is similar to the form of research report. Please include the specific numbers, types and specific trends in the picture, which will be very boring. Your customers are a group of college students."
        },
        {
          "type": "image_url",
          "image_url": {
            "url": f"data:image/jpeg;base64,{base64_image}"
          }
        }
      ]
    }
  ],
  "max_tokens": 300
}

response = requests.post("https://api.openai.com/v1/chat/completions", headers=headers, json=payload)

print(response.json())

